largest = None
smallest = None

while True:
    input = raw_input('>')
    if input == 'done':
        break
    try:
        number = float(input)
        if largest is None or number > largest:
            largest = number
        if smallest is None or number < smallest:
            smallest = number
    except:
        print 'Error'
        continue

print largest,smallest
